<!--
Author:         Shawn Hillyer
Description:    CS290 Assignment "GET and Post Checker"
Date;           2/12/2016
Note:           I used the lectures and textbook as 
reference, I did not cite every similarity because there
are only so many ways to do these things :)
-->
<h1 class="bg-primary">{{this.typeName}} request received</h1>

<!-- Print table of key value pairs from query string if they exist -->
<div class="row">
    <div class="col-lg-12">
        {{#if queryKvPairs}}
            <table class="table table-striped table-responsive">
                <caption>Query String Parameters</caption>
                <thead>
                    <th>Key</th>        
                    <th>Value</th>
                </thead>
                {{#each queryKvPairs}}
                <tr>
                    <td>{{@key}}</td>
                    <td>{{this}}</td>
                </tr>
                {{/each}}
            </table>
        {{else}}
            <p>No query string submitted</p>
        {{/if}}
    </div> <!-- End column -->
</div> <!-- End row -->

<!-- Print POST body parameters if they exist -->
<div class="row">
    <div class="col-lg-12">
        {{#if postKvPairs}}
            <table class="table table-striped table-responsive">
                <caption>POST Body Parameters</caption>
                <thead>
                    <th>Key</th>        
                    <th>Value</th>
                </thead>
                {{#each postKvPairs}}
                <tr>
                    <td>{{@key}}</td>
                    <td>{{this}}</td>
                </tr>
                {{/each}}
            </table>
        {{else}}
            <p>No req.body submitted</p>
        {{/if}}
    </div> <!-- End column -->
</div> <!-- End row -->
